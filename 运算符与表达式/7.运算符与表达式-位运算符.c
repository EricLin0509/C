// Copyright (C) 2024 EricLin
// SPDX-License-Identifier: GPL-3.0-only

#include  <stdio.h> 

int main(int argc, const char * argv[] ) {

int x = 6; // 6的二进制表示为 00000110
int y = 12; // 12的二进制表示为 00001100

int z = x & y; // 结果为 00000100，即4
// & 是按位与运算符，它会将两个操作数的每一位进行比较，如果两个操作数的对应位都为1，则结果的对应位为1，否则为0

printf("x & y = %d\n", z);


z = x | y; // 结果为 00001110，即14
// | 是按位或运算符，它会将两个操作数的每一位进行比较，如果两个操作数的对应位至少有一个为1，则结果的对应位为1，否则为0


z = x ^ y; // 结果为 00001010，即10
// ^ 是按位异或运算符，它会将两个操作数的每一位进行比较，如果两个操作数的对应位不同，则结果的对应位为1，否则为0

printf("x ^ y = %d\n", z);


z = x << 1; // 结果为 00011000，即24
// << 是左移运算符，它会将操作数的每一位向左移动指定的位数，右边空出的位用0填充

printf("x << 1 = %d\n", z);


z = x >> 1; // 结果为 00000011，即3
// >> 是右移运算符，它会将操作数的每一位向右移动指定的位数，左边空出的位用0填充

printf("x >> 1 = %d\n", z);


z = x >> 2; // 结果为 00000001，即1，移动2位相当于除以4，向下取整

printf("x >> 2 = %d\n", z);


z = ~x; // 结果为 11111001，即-7，~ 是按位取反运算符，它会将操作数的每一位取反，即0变为1，1变为0

printf("~x = %d\n", z);

return 0;

}